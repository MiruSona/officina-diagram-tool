# 할 일 : 설명용 그림 툴

> 담당 범위 : **우리끼리 보는** 기획·설계용 그림 (레벨 도면, 타임라인, 상태도, 와이어프레임)
> 전체 색인은 스튜디오 저장소(Officina)의 진행상황 문서에 있다

## 0. 지금 상태 — **0~5번 다 만들었고 한 번 검토했다**

만들 것 0~5번이 다 끝났고 검사 4a 도 붙였다. 완료 판정 3·6번과 엔진 C 의 C-1~C-4 를 통과했다 (아래 3장).
**남은 것은 뒤로 미룬 방언뿐이다** — 방 템플릿(검사 5~8)과 소코반(검사 9). 그 장르를 실제로 다룰 때 붙인다.

> **2026-08-21 검토** (`Research/2026-08-21-설명용그림툴검토.md`)
> 설계는 다시 봐도 맞았다. **구현에 놓치는 것이 하나 있었다** — 검사 4a 가 점프 곡선의 `x_h` 를 안 읽어서
> 견본 도면 하나가 **실제로 못 깨는 도면인데 "문제 없음"** 이 나왔다. 고쳤다.
> 같이 **회귀 시험 `Test/test.js`** 를 만들었다. 이제 검사가 조용히 죽으면 시험이 잡는다.

> **2026-08-21 확장** (`Research/2026-08-21-설명용그림툴확장조사.md` · `Design/2026-08-21-설명용그림툴확장설계.md`)
> 실사용을 시작하고 나서 구멍 넷을 더 메웠다 — **검사 10(크리티컬 패스)** · **검사 11(쉼터 없음)** ·
> **`예시=참` 블록 건너뛰기** · **`src/all.js`** (`Docs/` 전체 훑기).

> **2026-08-21 확장 검토** — 설계 항목은 다섯 다 들어가 있었다. **버그 하나가 있었다** :
> `all.js` 가 프로필 이름 오타를 만나면 **스택 트레이스를 뱉고 통째로 멎었다.** 오류 한 줄로 바꿨다.
> 같이 고친 것 — 없는 폴더를 적으면 exit 1 · 검사 10 이 "못 간 자리 앞 비트" 를 틀리게 짚던 것 ·
> 줄 끝 공백 알림이 `' ' 는 문자표에 없다` 라 못 알아듣던 것 · `쉼터간격` 오타를 조용히 삼키던 것.
> 회귀 시험을 76 → **83개**로 늘렸다.

| 단계 | 문서 | 상태 |
| --- | --- | --- |
| 조사 | `Research/2026-08-19-설명용그림툴조사.md` (도구 후보) | 끝 |
| 조사 | `Research/2026-08-20-게임기획서구성조사.md` (무슨 그림이 필요한가) | 끝 |
| 조사 | `Research/2026-08-20-횡스크롤액션설계도면조사.md` | 끝 |
| 조사 | `Research/2026-08-21-장르별설계도면조사.md` (시뮬·로그라이크·퍼즐·덱빌딩) | 끝 |
| 설계 | `Design/2026-08-20-설명용그림툴설계.md` | 끝 (2026-08-21 축 3개 + 장르 확장 반영) |
| 만들기 | `src/` (tilemap · timeline · lint · wireframe) + `Guide/` 문서 3장 | **0~5번 끝** |
| 검토 | `Research/2026-08-21-설명용그림툴검토.md` | 끝 (고칠 것 15개 고침) |
| 수정 내역 | `History/2026-08-21-그림툴수정내역.md` | **다른 에이전트 확인용.** 확인 절차와 이의 제기할 곳 |
| 조사 | `Research/2026-08-21-설명용그림툴확장조사.md` (실사용 후 구멍 찾기) | 끝 |
| 설계 | `Design/2026-08-21-설명용그림툴확장설계.md` (검사 10·11 등) | 끝 |

**게임에 들어가는 UI 그림은 이 툴이 아니다.** 그건 `그림툴.md` 의 UI 툴이다.

## 1. 정해진 것

| 항목 | 결정 |
| --- | --- |
| 엔진 3개 | **A. 격자·평면** / **B. 시간축** / **C. 관계 표 린터** |
| **툴을 안 쪼갠다** | 장르별로 나누지 않는다. 장르 차이는 **도면 프로필**(범례 + 켤 검사 목록)로 흡수 |
| **C 는 검사 도구** | 그림은 Mermaid 가 그린다. 우리는 표를 훑어 구멍을 잡는다 |
| 타일맵의 정체 | 그림이 아니라 **2차원 배열**. 그래서 AI 가 써도 안전하다 |
| 표기법 | 문자 그리드 + 범례. 코드펜스 태그는 ` ```tilemap ` |
| **축 3개 반영** | 도면을 바꾸는 건 **이동 축 하나뿐.** 투영은 렌더러만, 방향 수는 무관 |
| 격자의 뜻 | 중력+점프 = **옆에서 본 단면** / 평면 = **위에서 본 평면도** |
| 검증기 4번 | 중력+점프 = 점프 아크 / 평면 = **이어짐 검사(flood fill)** |
| Mermaid | 상태도·흐름도는 만들지 말고 **Mermaid 규칙만 정한다** |
| 방언 | `roomtemplate`(방 템플릿, 출입구 4방향) · `puzzle=sokoban`(XSB 표준 그대로) |
| 표기 문자 | **정본은 ASCII.** 눈에 편한 건 렌더러가 한다 (네모 기호는 폭이 모호해 정렬이 깨진다) |
| 곡선 그래프 | **안 만든다 (확정).** 조사 세 편이 각각 "표로 한다"고 확인 |
| 안 만들 것 | 곡선, 정밀 히트박스, 콤보 트리, 아스키 박스 그림, **시뮬레이터·밸런스 봇·던전 생성기** |
| 만들 언어 | **Node** (렌더 결과가 HTML 이라 자연스럽다) |
| **검사 10(크리티컬 패스)은 평면 프로필에서만** | 중력은 점프 아크 모델을 단계별로 돌려야 해서 값어치보다 비싸다 |
| **크리티컬 패스 비트 번호는 1~9** | 한 화면에 열 개 넘게 찍을 일이 없고, 두 글자를 쓰면 한 칸=한 글자 규칙이 깨진다 |
| **쉼터 한도는 3, 머리말 `쉼터간격` 으로 바꾼다** | 수치 근거를 못 찾은 임의값이라 바꿀 수 있게 열어 둔다 |
| **예시 표시는 코드펜스 머리말에 `예시=참`** | 표시 하나가 블록 단위로 갈린다. 파일 이름이나 코드 대상 폴더로 가르면 못 가른다 |
| **`src/all.js` 기본 대상은 지금 있는 폴더** | 폴더를 적으면 그 폴더만 본다. 일부러 틀린 견본이 든 `Test/` 는 `test.js` 가 본다 |
| **순환 검사** | 순환은 장르마다 뜻이 다르다. 테크 트리=버그, 생산 사슬=정상. **프로필로 켜고 끈다** (`Research/2026-08-21-장르별설계도면조사.md`) |
| **프로필 개수** | 게임당 하나가 아니다. **씬·모드마다 여러 개** (〃) |
| **그림 재사용** | **투영이 다르면 재사용 불가.** 기반 에셋을 투영별로 갖춘다 (〃) |
| **표 서식** | 기준선·타임라인·사슬·카드 표 서식은 `Guide/도면서식.md` (`Design/2026-08-20-설명용그림툴설계.md` 3-2·4·5장) |

## 2. 만들 순서

`Design/2026-08-20-설명용그림툴설계.md` 2장 기준.

- [x] **0. `Guide/그림그리기.md`** — Mermaid 사용 규칙. 만들 게 아니라 **정할 것**
- [x] **1. 타일맵** — `Guide/타일기호.md` (기호 확정) → 검증기 → 렌더러
  - [x] 도면 프로필 `src/profiles.json` — 범례 + 켤 검사 목록. **장르가 늘면 여기에 더한다**
  - [x] 검사 1~3 (줄 길이 / 미등록 문자 / 시작점 1개)
  - [x] **검사 4b 평면 이어짐** (문·열쇠 순서 꼬임까지 잡는다)
  - [x] 검사 4a 중력+점프 아크 (baseline 표에서 점프 값을 읽는다)
  - [x] HTML 격자 렌더러 + 터미널 미리보기 (정사각. 아이소 마름모는 뒤로)
  - [x] 시험 견본 `Test/시험견본.md` · 실제 구간 `Test/견본.md`
- [x] **2. 이동 기준선 표 서식** — `Guide/도면서식.md` 에 중력용 / 평면용 두 벌
- [x] **3. 프레임 타임라인 렌더러** — `timeline.js`. 프레임 · 페이싱 · 개수 분포 세 가지
- [x] **4. 와이어프레임 CSS** — `wireframe.css` + `Test/견본-HUD.html`
- [x] **5. 엔진 C — 관계 표 린터** — `lint.js`
  - [x] 표 서식 (`chain` · `cards` · `tech`)
  - [x] 검사 C1~C13
  - [x] 표 → Mermaid `flowchart` 변환

**뒤로 미룬 것** : 방 템플릿 방언 + 검사 5~8, 소코반 방언 + 솔버(검사 9).
1번이 끝난 뒤 **실제로 그 장르를 다룰 때** 붙인다.

## 3. 완료 판정 — **다 통과했다 (2026-08-21)**

| 판정 | 결과 | 어떻게 확인했나 |
| --- | --- | --- |
| 못 가는 곳을 잡나 (평면) | 통과 | `시험견본.md` 4·5번. 실제 구간에서 열쇠를 빼자 잡았다 |
| 못 가는 곳을 잡나 (중력) | 통과 | `시험견본.md` 7번. 5칸 위 발판을 잡았다 |
| 레벨 한 구간을 실제로 그린다 | 통과 | `견본.md` (평면 25x11) · `견본-중력.md` |
| 타임라인이 표에서 막대를 만드나 | 통과 | `견본-타임라인.md` |
| C-1 닭-달걀을 잡나 | 통과 | `시험견본-사슬.md` 1번 |
| **C-2 정상 순환을 오류로 안 뱉나** | **통과** | `견본-사슬.md` — 곡괭이 순환을 안 잡는다 |
| C-3 고아 카드를 잡나 | 통과 | `시험견본-사슬.md` 2번 |
| C-4 표에서 Mermaid 가 나오나 | 통과 | `lint.js --mermaid` |
| **회귀 시험이 검사 죽은 것을 잡나** | 통과 | `test.js` 40개. 검사 하나를 일부러 망가뜨려 잡히는 것을 봤다 |
| 어두운 화면에서 안 깨지나 | 통과 | 크롬 headless 로 찍어서 봤다. 타일맵·타임라인·와이어프레임 셋 다 |
| Mermaid 가 그려지나 | **확인 못 함** | `Test/견본-머메이드.md` 를 사람이 미리보기로 봐야 안다 |

## 3-1. 남은 것 (2026-08-21 검토에서 나온 것)

- **`견본-타임라인.md` 는 `견본*` 인데 알림이 2개 난다** (강도 5 세 번 · `5` 칸 0개).
  `Test/README.md` 규칙은 `견본*` = 오류 0개다. **일부러 알림을 보여 주려고 만든 표라 규칙과 어긋난다.**
  회귀 시험이 그 알림을 기대값으로 쓰고 있어서 이번엔 안 건드렸다. 표를 나누거나 이름을 바꿀 것.
- **검사 11 은 프로필로 못 켜고 끈다.** `timeline.js` 가 `profiles.json` 을 안 읽는다.
  장르마다 쉼터 기준이 달라지면 그때 프로필로 옮긴다.
- **검사 10 은 "아직 안 만난 뒤 비트를 벽으로 놓는다".** 설계 1-3 의 짜임새보다 한 칸 더 빡빡하다.
  `P 2 1 3` 처럼 늘어놓은 도면을 잡으려고 그렇게 했고, 시험은 다 통과한다. **설계 문서에는 안 적혀 있다.**

## 4. 아직 못 정한 것

- 엔진 C 표를 마크다운 표로 받을지 CSV 로 받을지
- 색 있는 문·열쇠 짝 (빨간 열쇠는 빨간 문만). 지금은 짝 없는 방식만 쓴다
- 열쇠가 모자랄 때 **어느 문을 먼저 열지** — 지금은 먼저 만난 문부터 연다. 드물게 최선이 아닐 수 있다
- 도달 가능성 검사 범위 — 평면은 대각선·대시, 중력은 더블점프. **둘 다 처음엔 뺀다**
- **카드 표에 희귀도 열을 넣을까** — C12 를 설계대로(낮은 희귀도만) 하려면 필요하다
- **생산 사슬 표에 건물 채수 열을 넣을까** — C7 수지를 실제 규모로 재려면 필요하다. 지금은 건물마다 1채로 본다
- **적 로스터 매트릭스를 `cards` 린터에 넣을까** — 역할×속성 표로 로스터 빈틈을 보는 것. 표 열 이름(`아키타입` 자리에 `역할`)을 정해야 한다
- **중력 도면에도 크리티컬 패스를 넣을까** — 이번엔 평면만 했다. 점프 아크 모델을 단계별로 돌려야 해서 값이 든다

**정해진 것 (2026-08-21)**
- **점프 모델은 여기까지 한다.** 더 다듬지 않고, **실제로 게임을 만들어 돌려 볼 때** 고친다.
  지금 모델은 되돌리는 점프를 빡빡하게 재고, 더블 점프·대시·벽타기를 안 본다. **알고 남긴 한계다**
- **카드 표에 희귀도 열을 넣는다.** C12 한도를 희귀도별로 잰다 (일반 40 · 고급 60 · 희귀 90 · 전설 130)
- **생산 사슬 표에 채수 열을 넣는다.** C7 수지가 채수를 곱해서 잰다
- **견본과 시험은 `Test/` 한 곳에 둔다.** `node Test/test.js` 하나로 다 돌린다
- 타일 기호와 보기 매핑 → `Guide/타일기호.md`
- 도면 프로필은 **그림 툴 프로필과 파일을 따로 둔다** (`src/profiles.json`). 읽는 쪽이 다르다
- 곡선 그래프 — **안 만든다 (확정).** 실제로 아쉬워지면 그때 다시 본다
- **검사를 어느 문서에 돌릴까 → 정해짐.** `node src/all.js <폴더>` 로 문서 폴더를 훑는다 (폴더를 안 적으면 지금 폴더). `Test/` 는 `test.js` 가 본다. 가이드의 예시 조각은 머리말 `예시=참` 으로 건너뛴다

## 5. 파일 배치

**툴 하나 = 최상위 폴더 하나.** `DiagramTool/` 안에서 다 끝난다. 바깥을 가리키는 경로는 없다.

```
DiagramTool/
  README.md                  <- 한 줄 요약 · 폴더 지도 · 쓰는 법

  src/common.js              <- 코드펜스·표 읽기 공통
  src/profiles.json          <- 도면 프로필 (범례 + 켤 검사)
  src/tilemap.js             <- 엔진 A. 검사 1~3 · 4a · 4b + 렌더러
  src/timeline.js            <- 엔진 B. 프레임 · 페이싱 · 개수 분포
  src/lint.js                <- 엔진 C. C1~C13 + Mermaid
  src/all.js                 <- 폴더 통째로 훑기 (인자 없으면 지금 폴더)
  src/wireframe.css          <- 와이어프레임 클래스
  src/solver.js              <- 소코반 솔버 (아직. 그 장르를 다룰 때)

  Test/test.js               <- 회귀 시험. 검사가 죽으면 여기서 잡는다
  Test/README.md             <- 견본 두 갈래 설명 · 다시 뽑는 법
  Test/견본*.md              <- 통과해야 하는 도면
  Test/시험견본*.md          <- 일부러 틀린 도면
  Test/견본.html · 견본-타임라인.html · 견본-HUD.html · 견본-머메이드.md
                             <- 눈으로 보는 결과물

  Docs/Guide/               <- 그림그리기.md (Mermaid 규칙, AI 가 읽음)
                               타일기호.md   (타일맵 문자 약속)
                               도면서식.md   (표 서식 : 기준선·타임라인·페이싱·분포·사슬·카드)
  Docs/Research/             <- 조사 6장
  Docs/Design/               <- 설계 2장
  Docs/Todo/설명용그림툴.md  <- 이 문서
  Docs/History/              <- 수정 내역
```

`DiagramTool/` 을 `Assets/` 밖에 두는 이유 : Unity 가 `Assets/` 안만 본다.
