# 할 일 : 설명용 그림 툴

> 담당 범위 : **우리끼리 보는** 기획·설계용 그림 (레벨 도면, 타임라인, 상태도, 와이어프레임)
> 전체 색인은 `Docs/Todo/진행상황.md`

## 0. 지금 상태 — **조사·설계 끝. 만들 차례**

그림 툴 3종보다 앞서 있다. 바로 만들기에 들어갈 수 있다.

| 단계 | 문서 | 상태 |
| --- | --- | --- |
| 조사 | `Research/2026-08-19-설명용그림툴조사.md` (도구 후보) | 끝 |
| 조사 | `Research/2026-08-20-게임기획서구성조사.md` (무슨 그림이 필요한가) | 끝 |
| 조사 | `Research/2026-08-20-횡스크롤액션설계도면조사.md` | 끝 |
| 설계 | `Design/2026-08-20-설명용그림툴설계.md` | 끝 (2026-08-21 축 3개 반영) |
| 만들기 | — | **여기부터** |

**게임에 들어가는 UI 그림은 이 툴이 아니다.** 그건 `그림툴.md` 의 UI 툴이다.

## 1. 정해진 것

| 항목 | 결정 |
| --- | --- |
| 엔진 2개 | **A. 격자·평면** / **B. 시간축**. 이 둘로 필요한 그림 대부분을 덮는다 |
| 타일맵의 정체 | 그림이 아니라 **2차원 배열**. 그래서 AI 가 써도 안전하다 |
| 표기법 | 문자 그리드 + 범례. 코드펜스 태그는 ` ```tilemap ` |
| **축 3개 반영** | 도면을 바꾸는 건 **이동 축 하나뿐.** 투영은 렌더러만, 방향 수는 무관 |
| 격자의 뜻 | 중력+점프 = **옆에서 본 단면** / 평면 = **위에서 본 평면도** |
| 검증기 4번 | 중력+점프 = 점프 아크 / 평면 = **이어짐 검사(flood fill)** |
| Mermaid | 상태도·흐름도는 만들지 말고 **Mermaid 규칙만 정한다** |
| 안 만들 것 | 난이도 곡선, 점프 아크 곡선, 정밀 히트박스 도형, 콤보 트리, 아스키 박스 그림 |
| 만들 언어 | **Node** (렌더 결과가 HTML 이라 자연스럽다) |

## 2. 만들 순서

`Design/2026-08-20-설명용그림툴설계.md` 2장 기준.

- [ ] **0. `Guide/그림그리기.md`** — Mermaid 사용 규칙. 만들 게 아니라 **정할 것**
- [ ] **1. 타일맵** — `Guide/타일기호.md` (기호 확정) → 검증기 → 렌더러
  - [ ] 검사 1~3 (줄 길이 / 미등록 문자 / 시작점 1개)
  - [ ] **검사 4b 평면 이어짐 — 먼저 만든다** (기본 프로필이 평면)
  - [ ] 검사 4a 중력+점프 아크 — 나중
  - [ ] HTML 격자 렌더러 (정사각. 아이소 마름모는 뒤로)
- [ ] **2. 이동 기준선 표 서식** — 중력용 / 평면용 두 벌. 표라서 서식만 정하면 끝
- [ ] **3. 프레임 타임라인 렌더러** — 표 → 막대
- [ ] **4. 와이어프레임 CSS** — 엔진 A 재활용. 빈도는 높지만 종류가 적어 뒤로

## 3. 완료 판정

**아래 2개를 통과해야 끝난 것으로 본다.**

- 검증기가 **"못 가는 곳"** 을 잡아내나 (평면 = 이어짐 / 중력 = 발판)
- **실제로 레벨 한 구간을 그려본다** — 기본 프로필(쿼터뷰·4방향·평면)

나머지 판정 항목은 설계 문서 8장.

## 4. 아직 못 정한 것

- 타일 기호 최종 확정 (`Guide/타일기호.md`)
- 도달 가능성 검사 범위 — 평면은 대각선·대시, 중력은 더블점프. **둘 다 처음엔 뺀다**
- 곡선 그래프 — **보류.** 실제로 아쉬워지면 그때 만든다 (설계 문서 6장)

## 5. 파일 배치

```
Docs/Guide/그림그리기.md      ← Mermaid 규칙 (AI 가 읽음)
Docs/Guide/타일기호.md        ← 타일맵 문자 약속
Docs/Design/Wireframe/*.html  ← 그린 결과물
Tools/Draw/                   ← tilemap.js · timeline.js · wireframe.css
```

`Tools/` 를 `Assets/` 밖에 두는 이유 : Unity 가 `Assets/` 안만 본다.
